{
  "name": "codebuddy-gateway-console",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "CodeBuddy Gateway 管理控制台（Vue 3 + Naive UI + Vite）",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/check-tokens.mjs && node scripts/check-style-collisions.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "check:styles": "node scripts/check-style-collisions.mjs",
    "check:tokens": "node scripts/check-tokens.mjs",
    "typecheck": "vue-tsc --noEmit && node scripts/check-tokens.mjs && node scripts/check-style-collisions.mjs"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "naive-ui": "^2.40.1",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vite-plugin-compression2": "^2.5.3",
    "vue-tsc": "^2.2.12"
  }
}
