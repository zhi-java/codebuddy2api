{
  "name": "codebuddy-gateway",
  "version": "1.2.0",
  "description": "Node.js proxy gateway for CodeBuddy chat completions",
  "main": "src/server.ts",
  "scripts": {
    "dev": "npm run build && npm run start",
    "build": "esbuild src/server.ts --bundle --platform=node --format=cjs --outfile=dist/server.cjs",
    "build:web": "npm --prefix web ci && npm --prefix web run build",
    "build:all": "npm run build:web && npm run build",
    "start": "node dist/server.cjs",
    "test": "node tests/run-tests.mjs",
    "typecheck": "tsc --noEmit && npm --prefix web run typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "esbuild": "^0.28.2",
    "typescript": "^5.7.0"
  }
}
